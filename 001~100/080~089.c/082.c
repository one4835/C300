#include<stdio.h>
#include<string.h>

void main(void)
{
    char*string = "1.234E-10";
    char*stop;
    double value;

    printf("%d of the words are changed.\n",stop - string);
    printf("It's %ld that words[%s] changed into number\n",string,value);
}
